name: Build & Push Docker Images

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_REPOSITORY: ${{ github.repository_owner }}/sansons-e-commerce
  BACKEND_IMAGE: ghcr.io/${{ env.IMAGE_REPOSITORY }}/backend
  FRONTEND_IMAGE: ghcr.io/${{ env.IMAGE_REPOSITORY }}/frontend

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push Backend image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile.backend
          push: true
          tags: |
            ${{ env.BACKEND_IMAGE }}:latest
            ${{ env.BACKEND_IMAGE }}:${{ github.sha }}

      - name: Build & push Frontend image
        uses: docker/build-push-action@v5
        with:
          context: ./sansons-frontend
          file: ./sansons-frontend/Dockerfile
          push: true
          build-args: |
            NEXT_PUBLIC_API_URL=/api/
            NEXT_PUBLIC_GOOGLE_CLIENT_ID=60165557205-2avhk0lr636jviob6rb2hri0cijnka7r.apps.googleusercontent.com
          tags: |
            ${{ env.FRONTEND_IMAGE }}:latest
            ${{ env.FRONTEND_IMAGE }}:${{ github.sha }}
